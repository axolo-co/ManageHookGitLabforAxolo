# Recommended name. The lowercase `pat` alias is also supported.
GITLAB_ACCESS_TOKEN=replace-with-your-gitlab-token

# Optional for GitLab Self-Managed or GitLab Dedicated.
# GITLAB_API_URL=https://gitlab.example.com/api/v4
